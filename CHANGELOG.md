# Changelog

## Unreleased

### Chores
- Remove unused build artifact `dist/bundle.js` and unused client code (`src/client/commands/*`, `src/client/entities/PowerUp.js`).
- Add `.gitignore` entries for `/dist` and `/node_modules`.
- Add minimal `Paddle` entity to support multiplayer scene.
- Run ESLint fixes and clean up empty catches and unused variables.
- Adjust tests to avoid unused parameter warnings.
